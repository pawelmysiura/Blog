<ul class="nav flex-column">
    {% for name, route in accountMenu %}
        {% if app.request.get('_route') == route %}
            <li class="nav-item active">
        {% else %}
            <li class="nav-item">
        {% endif %}
        <a class="nav-link" href="{{ path(route) }}">{{ name }}</a>
        </li>
        {% endfor %}
</ul>